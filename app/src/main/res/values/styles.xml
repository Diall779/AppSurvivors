<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorEdgeEffect">@color/colorEdgeEffect</item>
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <item name="colorControlActivated">@color/colorControlActivated</item>
        <item name="colorControlHighlight">@color/colorControlHighlight</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="drawerArrowStyle">@style/MyDrawerArrowToggle</item>
        <item name="actionBarStyle">@style/MyActionBar</item>

    </style>

    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/Toolbar.TitleText</item>
        <item name="android:background">@color/colorBackgroundToolbar</item>

        <!-- Support library compatibility -->
        <item name="titleTextStyle">@style/Toolbar.TitleText</item>
        <item name="background">@color/colorBackgroundToolbar</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/titleTextSize</item>
        <item name="android:fontFamily">@font/font_survivors</item>
        <item name="android:letterSpacing">0.1</item>
        <item name="colorControlNormal">@color/colorTextPrimary</item>
    </style>

    <style name="MyDrawerArrowToggle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/colorTextPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="tabTextFont" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">@font/font_survivors_2</item>
    </style>

    <style name="AlertDialogCustom" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:background">@color/colorBackgroundScreen</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="NavigationDrawerStyle">

        <item name="android:textSize">@dimen/textSizeNavigationMenu</item>
        <item name="android:fontFamily">@font/font_survivors_2</item>

        <!-- item size in menu-->
        <item name="android:listPreferredItemHeightSmall">45dp</item>
        <item name="listPreferredItemHeightSmall">45dp</item>

        <!-- item padding left in menu-->
        <item name="android:listPreferredItemPaddingLeft">0dp</item>
        <item name="listPreferredItemPaddingLeft">0dp</item>

        <!-- item padding right in menu-->
        <item name="android:listPreferredItemPaddingRight">8dp</item>
        <item name="listPreferredItemPaddingRight">8dp</item>
        <item name="itemIconTint">@color/colorTextPrimary</item>


    </style>

</resources>